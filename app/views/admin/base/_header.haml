%ol.breadcrumb.strong{:style => "border: 1px solid #ccc"}
    - *prefix, last = breadcrumb_trail
    - prefix.each do |name, href|
        %li
            %a{href: href}= name
    %li.active
        = last[0]
